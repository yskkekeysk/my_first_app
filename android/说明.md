


#   Android 开发工具运行指南
📱 一、Android 开发工具对比
    工具           	   用途                       	    适合场景
    Android Studio	   官方IDE，功能最全	               原生Android开发、Flutter开发、调试复杂问题
    VS Code            轻量级编辑器，Flutter官方推荐	      Flutter快速开发、Dart编程
    命令行工具	        快速操作                      	构建、打包、测试



#  二、Android Studio 运行 Flutter 项目
    🚀 完整安装运行流程
    步骤1：安装 Android Studio
        下载安装包：
        官网：https://developer.android.com/studio
        选择对应系统版本（Windows/macOS/Linux）
        安装过程：
        Windows：运行 .exe，按向导安装
        macOS：拖拽到 Applications 文件夹
        Linux：解压并运行 ./studio.sh
        首次运行配置：
              - 选择 "Do not import settings"（首次安装）
              - 选择 Standard 安装类型
              - 选择主题（Light/Dark）
              - 下载 SDK（Android 11/12 推荐）
    步骤2：安装 Flutter 插件
          打开插件市场：
          File → Settings → Plugins（Windows/Linux）
          Android Studio → Preferences → Plugins（macOS）
          搜索并安装：
              搜索 "Flutter"
              点击 "Install"
          同时安装 Dart 插件（会自动提示）
          重启 Android Studio：
          https://flutter.dev/assets/images/docs/development/tools/android-studio/install-flutter-plugin.png
    
    步骤3：配置 Flutter SDK
        打开配置：
        File → Settings → Languages & Frameworks → Flutter
        设置 SDK 路径：
        Flutter SDK path: [选择您的 Flutter 安装目录]
        例如：C:\flutter（Windows）或 /Users/username/flutter（macOS）
        验证配置：
        点击 "Apply" → "OK"
        打开终端输入：flutter doctor
    
    步骤4：打开 Flutter 项目
        欢迎界面打开：
        点击 "Open an existing Android Studio project"
        选择您的 my_first_app 文件夹
        或从菜单打开：
        File → Open
        选择项目目录
    
    步骤5：设置模拟器（AVD）
        打开 AVD Manager：
        点击工具栏的 AVD 图标 🎮
        或 Tools → AVD Manager
        创建虚拟设备：
        点击 "Create Virtual Device"
            选择设备类型（推荐 Pixel 4）
            选择系统镜像（API 33 或 34）
            点击 "Finish"
        启动模拟器：
            在 AVD Manager 中点击 "Play" 按钮
            或直接在 Android Studio 运行
    
    步骤6：运行 Flutter 项目
        选择运行配置：
        点击工具栏设备选择器
        选择您的模拟器或真机
        点击运行按钮：
        ▶️ 绿色三角形按钮
        或 Run → Run 'main.dart'
        或快捷键 Shift+F10
        等待构建：
        首次运行需要较长时间
        控制台显示构建进度


#  📱 六、连接真机调试
    Android 真机连接
    # 1. 开启开发者选项
    设置 → 关于手机 → 多次点击 "版本号"
    # 2. 开启USB调试
    设置 → 开发者选项 → USB调试
    # 3. 连接电脑
    使用数据线连接手机和电脑
    # 4. 授权连接
    手机弹出提示，点击 "允许"
    # 5. 验证连接
    flutter devices
    常见连接问题
    # 1. 设备未识别
    # Windows：安装手机驱动
    # macOS/Linux：通常自动识别
    # 2. ADB 连接问题
    adb kill-server
    adb start-server
    adb devices
    # 3. 权限问题
    # Linux：将用户添加到 plugdev 组
    sudo usermod -aG plugdev $USER

#  七、Android 项目配置
    修改应用配置
    # 文件位置：android/app/build.gradle
    android {
    compileSdkVersion 34  # 编译SDK版本
        defaultConfig {
            applicationId "com.example.myapp"  # 包名
            minSdkVersion 21                   # 最低支持版本
            targetSdkVersion 34                # 目标版本
            versionCode 1                      # 版本号（整数）
            versionName "1.0.0"                # 版本名
        }
    }
    修改应用图标
    准备图标：多尺寸 PNG 文件
    图标位置：android/app/src/main/res/
    使用工具：flutter pub add flutter_launcher_icons

#  八、调试技巧
    Android Studio 调试
    # 1. 设置断点
    在代码行号左侧点击
    # 2. 调试运行
    点击 🐞 按钮（Debug
    # 3. 查看变量
    在 Debug 面板查看变量
    # 4. 条件断点
    右键断点 → 设置条件
    日志查看
    bash
    # 1. 查看所有日志
    flutter logs
    # 2. 过滤日志
    flutter logs --filter "MyApp"
    # 3. 查看特定级别
    Log.d("TAG", "Debug message")  # Debug
    Log.i("TAG", "Info message")   # Info
    Log.e("TAG", "Error message")  # Error
