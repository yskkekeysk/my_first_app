# 以下步骤需要使用 PowerShell。

        在 PowerShell 中打开新窗口，准备运行 shell 命令。
            将 PUB_HOSTED_URL 设置为镜像站点。
                $ $env:PUB_HOSTED_URL="https://pub.flutter-io.cn"
            将 FLUTTER_STORAGE_BASE_URL 设置为镜像站点。
                $ $env:FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"

        从镜像站点下载 Flutter 压缩包。
            Stable channel
                请从以下列表中选择：
                Flutter 版本 架构 Ref 发布日期 Dart 版本 出处/来源
                3.38.9 x64 67323de 2026/1/30 3.10.8 Attestation bundle

        方法一：通过 Windows 系统设置界面
                打开环境变量设置
                    按 Win + R 键，输入 sysdm.cpl，点击"确定"
                    选择"高级"选项卡，点击"环境变量"按钮
                    添加用户变量
                    在"用户变量"区域，点击"新建"
                    第一个变量：
                    变量名：PUB_HOSTED_URL
                    变量值：https://pub.flutter-io.cn
                    再次点击"新建"，添加第二个变量：
                    变量名：FLUTTER_STORAGE_BASE_URL
                    变量值：https://storage.flutter-io.cn
                    添加 Flutter 到 PATH
                    在"用户变量"区域，找到并选择 Path 变量，点击"编辑"
                    点击"新建"，添加 Flutter 的 bin 目录路径
                    例如：C:\src\flutter\bin（根据你的实际安装路径）
                点击"确定"保存所有更改

          方案1：切换镜像源（推荐）
            方案2：使用代理（如果您有）
            powershell
            # 设置代理（端口号根据您的代理软件设置）
            $env:HTTP_PROXY="http://127.0.0.1:9876"
            $env:HTTPS_PROXY="http://127.0.0.1:9876"

            # 清除之前的镜像设置，尝试直连
            Remove-Item Env:PUB_HOSTED_URL
            Remove-Item Env:FLUTTER_STORAGE_BASE_URL

            # 再试一次
            flutter --version
            dart --version
            🚀 下一步：运行环境诊断与配置
                现在，你需要运行一个全面的环境检查，看看开发Android、iOS或Web应用还需要哪些组件。请运行以下命令：
                powershell
                flutter doctor


                1. Android 工具链问题
                        这个问题有两个子问题：
                        A. 接受 Android 许可证
                        flutter doctor --android-licenses
                        然后按 y 接受所有许可证。
                        B. 安装 Android SDK 命令行工具
                        下载 Android SDK 命令行工具：
                        访问：https://developer.android.com/studio#command-line-tools-only
                        下载适用于 Windows 的 commandlinetools-win-*.zip
                        解压并安装：
                        # 将下载的zip文件解压到以下目录：
                        # C:\Users\您的用户名\AppData\Local\Android\Sdk\cmdline-tools\latest

                        # 或者使用 Android Studio 安装：
                        # 1. 打开 Android Studio
                        # 2. 进入 Settings → Appearance & Behavior → System Settings → Android SDK
                        # 3. 选择 SDK Tools 选项卡
                        # 4. 勾选 "Android SDK Command-line Tools"
                        # 5. 点击 Apply

                2. Visual Studio 问题
                        Windows 开发需要完整的 Visual Studio 安装：
                        打开 Visual Studio Installer
                        点击 修改 (Modify)
                        确保以下工作负载已安装：
                        ✅ 使用 C++ 的桌面开发 (Desktop development with C++)
                        ✅ 通用 Windows 平台开发 (Universal Windows Platform development)
                        点击 修改 完成安装

                3. 代理配置问题
                    # 设置 NO_PROXY 环境变量
                    setx NO_PROXY "localhost,127.0.0.1,::1"
                    # 或者通过系统属性设置：
                    # 1. 右键点击"此电脑" → 属性
                    # 2. 高级系统设置 → 环境变量
                    # 3. 在系统变量中新建变量：
                    #    变量名：NO_PROXY
                    #    变量值：localhost,127.0.0.1,::1

                完整的修复步骤：
                    # 1. 接受 Android 许可证
                    flutter doctor --android-licenses
                    # 2. 安装 Android SDK 命令行工具（通过 Android Studio 或手动下载）
                    # 3. 通过 Visual Studio Installer 完善 Visual Studio 安装
                    # 4. 设置 NO_PROXY 环境变量
                    setx NO_PROXY "localhost,127.0.0.1,::1"
                    # 5. 重启命令行窗口
                    # 6. 再次检查 Flutter 状态
                    flutter doctor
                        C:\Users\YSK>flutter doctor
                            Flutter assets will be downloaded from https://storage.flutter-io.cn. Make sure you trust this source!
                            Doctor summary (to see all details, run flutter doctor -v):
                            [√] Flutter (Channel stable, 3.38.9, on Microsoft Windows [版本 10.0.26100.7623], locale zh-CN)
                            [√] Windows Version (Windows 11 or higher, 24H2, 2009)
                            [√] Android toolchain - develop for Android devices (Android SDK version 36.1.0)
                            [√] Chrome - develop for the web
                            [√] Visual Studio - develop Windows apps (Visual Studio Community 2022 17.14.13 (August 2025))
                            [√] Connected device (3 available)
                            [√] Network resources
                            • No issues found!

# ndk安装

            方式 C：手动解压并移动
                    双击 ZIP 文件打开
                    全选所有文件，拖拽到 G:\android_ios\Android\Sdk\ndk\29.0.14206865\
                    确保文件结构正确：
                    正确的文件结构：
                    G:\android_ios\Android\Sdk\ndk\29.0.14206865\
                    ├── ndk-build.cmd
                    ├── ndk-gdb.cmd
                    ├── ndk-stack.cmd
                    ├── ndk-which.cmd
                    ├── build/
                    ├── prebuilt/
                    ├── source.properties
                    ├── toolchains/
                    └── ...其他文件
            步骤 3：验证安装
                    # 打开命令提示符（CMD）或 PowerShell
                    # 执行以下命令验证 NDK 是否正确安装
                    "G:\android_ios\Android\Sdk\ndk\29.0.14206865\ndk-build.cmd" --version

                    # 应该看到类似输出：
                    # GNU Make 4.2.1
                    # Built for Windows32
                    # 或显示 NDK 版本信息

                G:\android_ios\Android\Sdk\ndk\29.0.14206865>"G:\android_ios\Android\Sdk\ndk\29.0.14206865\ndk-build.cmd" --version
                GNU Make 4.3
                Built for Windows32
                Copyright (C) 1988-2020 Free Software Foundation, Inc.
                License GPLv3+: GNU GPL version 3 or later <http://gnu.org/licenses/gpl.html>
                This is free software: you are free to change and redistribute it.
                There is NO WARRANTY, to the extent permitted by law.

# 4. 创建第一个 Flutter 项目测试

        powershell # 切换到项目目录
        cd $env:USERPROFILE
            # 创建新项目
            flutter create my_first_app
            # 进入项目
            cd my_first_app
            # 运行项目（确保连接了Android设备或模拟器）
            flutter run
            # 4. 如果要在 Chrome 中运行
                flutter run -d chrome

    # 5. 安装 VS Code 扩展（推荐）
        如果使用 VS Code：
        安装 Flutter 扩展
        安装 Dart 扩展
        按 F5 可以直接运行调试

    # 6. 配置 Android Studio / IntelliJ IDEA
        安装 Flutter 插件
        安装 Dart 插件
        配置 Flutter SDK 路径：$env:USERPROFILE\dev\flutter

    # 验证安装完成
        运行以下命令确认一切正常：
        powershell
        # 检查所有环境
        flutter doctor -v
        # 创建并运行示例项目
        flutter create --sample=widgets.LandingPage my_demo_app
        cd my_demo_app
        flutter run
    # 如果还有问题
            如果 flutter doctor 显示仍有问题，请运行：
            powershell
            # 获取详细错误信息
            flutter doctor -v
            # 尝试修复
            flutter doctor --android-licenses
    # 常用命令备忘
            powershell
            # 升级 Flutter
            flutter upgrade
            # 获取包依赖
            flutter pub get
            # 构建 APK
            flutter build apk
            # 运行测试
            flutter test
            # 清理项目
            flutter clean
    # 重启计算机
        完成所有配置后，建议重启计算机，以确保所有环境变量生效。
        重启后，在任何地方打开 PowerShell，都可以直接使用 flutter 命令了！
    # 总结： 您现在已经成功安装了 Flutter，接下来就是根据 flutter doctor 的提示配置其他开发环境（Android Studio、VS Code 等），然后就可以开始 Flutter 开发了。
